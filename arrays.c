#include<stdio.h>

int main(){
    // array is collection of similar items
    // it is like a variable that is capable of storing multiple items
    int marks[10] = {1,2,3,4,45,6,7,8,9};
     for (int i = 0; i<9; i++)
     {
        printf("%d \n",marks[i]);
     }
     
return 0;
}